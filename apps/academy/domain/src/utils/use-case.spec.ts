import { describe, test } from "@fabric/testing";

describe("UseCase", () => {
  test("Given a use case without permissions, when the user is anonymous, then the use case should be accessible", () => {
    // const useCase = new UseCase({
    //   name: "test",
    //   auth: AccessPolicy.ANONYMOUS(),
    //   effect: () => Effect.ok("OK"),
    //   inputSchema: undefined,
    // });
  });
});
