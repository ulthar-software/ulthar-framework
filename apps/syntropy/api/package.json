{
  "name": "@syntropy/api",
  "version": "0.0.1",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "dependencies": {
    "@fabric/core": "workspace:^"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.6"
  },
  "scripts": {
    "test:coverage": "vitest run --coverage",
    "test": "vitest run",
    "test:dev": "vitest",
    "build": "tsc -p tsconfig.build.json"
  }
}
